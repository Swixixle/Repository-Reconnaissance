# Internal documentation

This directory holds **non-user-facing** notes: contact templates, profile setup, typecheck debt, Replit runbooks, security audit snapshots, and the **cleanup log** from the 2026-03 naming pass. Session / verification artifacts live under **`process/`**. Public product documentation lives in `docs/` at the repo root (`ARCHITECTURE.md`, `CONFIGURATION.md`, `DEPLOYMENT.md`, etc.). Start with the root **`README.md`** for Debrief; use **`CLEANUP_LOG.md`** here to see what moved during professionalization.
